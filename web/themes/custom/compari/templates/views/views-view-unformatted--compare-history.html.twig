{#
/**
 * @file
 * Theme override for the Compare History unformatted rows.
 */
#}
<div id="compare-history-list" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 items-stretch">
  {% for row in rows %}
    {{ row.content }}
  {% endfor %}
</div>
