/**
 * @file
 * compari behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.compari = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
